model:
  _target_: walkjump.cmdline.utils.instantiate_model_for_sample_mode
  sample_mode_model_cfg:
    model_type: denoise
    checkpoint_path: /home/srinu_pd/checkpoints/last.ckpt
    denoise_path: null
langevin:
  sigma: 1.0
  delta: 0.5
  lipschitz: 1.0
  friction: 1.0
  steps: 20
designs:
  output_csv: samples.csv
  redesign_regions: null
  seeds: toy
  x_lim: 20
  dim: 2
  num_samples: 100
  limit_seeds: 10
  chunksize: 8
device: null
dryrun: false
setup:
  torch:
    _target_: torch.set_float32_matmul_precision
    precision: medium
  seed:
    _target_: lightning.pytorch.seed_everything
    seed: 15855310
    workers: true
